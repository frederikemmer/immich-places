/**
 * Delay in milliseconds before unmounting the lightbox overlay after close.
 */
export const LIGHTBOX_UNMOUNT_DELAY_MS = 200;

/**
 * Standardized DOM keyboard event name.
 */
export const KEYDOWN_EVENT = 'keydown';
/**
 * Escape key value used to close the lightbox.
 */
export const KEY_ESCAPE = 'Escape';
/**
 * Left arrow key value used to navigate to previous asset.
 */
export const KEY_ARROW_LEFT = 'ArrowLeft';
/**
 * Right arrow key value used to navigate to next asset.
 */
export const KEY_ARROW_RIGHT = 'ArrowRight';
