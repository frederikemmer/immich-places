/**
 * Tunable constants for assigning and confirming photo locations.
 */
/** Precision used when comparing user-confirmed coordinates. */
export const LOCATION_CONFIRM_COORDINATES_DECIMALS = 5;
/** Delay in milliseconds between each save request while persisting multiple assets. */
export const LOCATION_SAVE_REQUEST_DELAY_MS = 100;
